services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: codetrace
      POSTGRES_PASSWORD: codetrace
      POSTGRES_DB: codetrace
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
